<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete User</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: linear-gradient(135deg, #ffdde1, #ee9ca7, #b5ead7);
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }
      .container {
        background: white;
        padding: 20px;
        border-radius: 15px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        text-align: center;
        width: 350px;
      }
      h3 {
        color: #d72638;
        font-weight: bold;
      }
      input {
        width: 100%;
        padding: 10px;
        margin-top: 10px;
        border: 1px solid #ccc;
        border-radius: 8px;
      }
      button {
        background-color: #d72638;
        color: white;
        border: none;
        padding: 12px;
        margin-top: 15px;
        width: 100%;
        border-radius: 8px;
        cursor: pointer;
        font-size: 16px;
        transition: 0.3s;
      }
      button:hover {
        background-color: #a91d2a;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h3>To delete this user: <%= user.email %>, enter the password</h3>
      <form method="post" action="/user/<%= user.id%>?_method=DELETE">
        <input placeholder="Enter password to delete" name="password" type="password" required />
        <button>Delete</button>
      </form>
    </div>
  </body>
</html>
